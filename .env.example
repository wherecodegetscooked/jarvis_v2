# Standard server-side OpenAI credential. Never expose this in browser code.
# This is the only required secret. Wake-word detection runs locally in the
# browser via openWakeWord and needs no key.
OPENAI_API_KEY=

PORT=3000
HOST=127.0.0.1
JARVIS_VOICE=cedar
JARVIS_MODEL=gpt-realtime-2.1
