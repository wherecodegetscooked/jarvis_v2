{
  "name": "jarvis-voice-satellite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/prepare-assets.mjs",
    "dev": "NODE_ENV=development node server.mjs",
    "build": "tsc --noEmit && vite build",
    "start": "NODE_ENV=production node server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "onnxruntime-web": "1.23.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  },
  "engines": {
    "node": ">=22"
  }
}
